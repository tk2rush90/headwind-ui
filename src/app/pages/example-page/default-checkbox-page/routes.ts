import { Routes } from '@angular/router';
import { DefaultCheckboxPageComponent } from './default-checkbox-page.component';

export const routes: Routes = [
  {
    path: '',
    component: DefaultCheckboxPageComponent,
  },
];
