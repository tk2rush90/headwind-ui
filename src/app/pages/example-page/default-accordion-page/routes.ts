import { Routes } from '@angular/router';
import { DefaultAccordionPageComponent } from './default-accordion-page.component';

export const routes: Routes = [
  {
    path: '',
    component: DefaultAccordionPageComponent,
  },
];
