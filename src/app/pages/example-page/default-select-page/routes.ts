import { Routes } from '@angular/router';
import { DefaultSelectPageComponent } from './default-select-page.component';

export const routes: Routes = [
  {
    path: '',
    component: DefaultSelectPageComponent,
  },
];
