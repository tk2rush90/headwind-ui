import { HeadwindOptionsOverlayDirective } from './headwind-options-overlay.directive';

describe('HeadwindOptionsTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadwindOptionsOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
