import { HeadwindOptionsTemplateDirective } from './headwind-options-template.directive';

describe('HeadwindOptionsTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadwindOptionsTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
