import { HeadwindAccordionContentDirective } from './headwind-accordion-content.directive';

describe('HeadwindAccordionContentDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadwindAccordionContentDirective();
    expect(directive).toBeTruthy();
  });
});
