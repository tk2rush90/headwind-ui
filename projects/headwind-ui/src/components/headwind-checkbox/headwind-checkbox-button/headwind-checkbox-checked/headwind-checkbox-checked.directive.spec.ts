import { HeadwindCheckboxCheckedDirective } from './headwind-checkbox-checked.directive';

describe('HeadwindCheckboxCheckedDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadwindCheckboxCheckedDirective();
    expect(directive).toBeTruthy();
  });
});
