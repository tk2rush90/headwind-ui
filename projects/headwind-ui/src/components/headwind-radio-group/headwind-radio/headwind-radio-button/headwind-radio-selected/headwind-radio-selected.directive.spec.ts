import { HeadwindRadioSelectedDirective } from './headwind-radio-selected.directive';

describe('HeadwindRadioSelectedDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadwindRadioSelectedDirective();
    expect(directive).toBeTruthy();
  });
});
