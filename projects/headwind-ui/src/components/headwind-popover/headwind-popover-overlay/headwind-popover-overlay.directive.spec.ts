import { HeadwindPopoverOverlayDirective } from './headwind-popover-overlay.directive';

describe('HeadwindPopoverOverlayTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadwindPopoverOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
