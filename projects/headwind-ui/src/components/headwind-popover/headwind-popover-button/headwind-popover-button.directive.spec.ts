import { HeadwindPopoverButtonDirective } from './headwind-popover-button.directive';

describe('HeadwindPopoverButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadwindPopoverButtonDirective();
    expect(directive).toBeTruthy();
  });
});
