import { HeadwindPopoverOverlayTemplateDirective } from './headwind-popover-overlay-template.directive';

describe('HeadwindPopoverOverlayTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadwindPopoverOverlayTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
