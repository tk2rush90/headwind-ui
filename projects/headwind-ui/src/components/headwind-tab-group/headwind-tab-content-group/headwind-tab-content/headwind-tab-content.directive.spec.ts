import { HeadwindTabContentDirective } from './headwind-tab-content.directive';

describe('HeadwindTabContentDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadwindTabContentDirective();
    expect(directive).toBeTruthy();
  });
});
